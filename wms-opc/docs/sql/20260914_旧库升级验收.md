# 旧库升级SQL验收（20260916-r5）

> 历史记录：本文记录开源迁移前的测试或交付，地址已脱敏。开源版初始密码和维护账号配置以[项目首页](../../../README.md)及[登录与权限](../用户登录与权限.md)为准。

## 本次安全更新兼容修复验收

本次在本机隔离 MySQL 8.0.45 开启 SQL_SAFE_UPDATES，完成9项针对性验收，全部通过。未连接或修改现场、27、149数据库；未重新执行历史百万行/应用验收，下方原r3/r4报告保留作为历史证据。

| 本次场景 | 结果 |
| --- | --- |
| r4旧脚本 + 安全更新开启 | 成功复现1175 |
| 原连接、原半升级数据库切换r5完整重跑 | 升级成功，安全更新恢复为1，旧字段值保留 |
| 完成后再次完整运行r5 | 12表数据一致，安全更新仍为1 |
| 升级后只读核验 | 所有violations为0 |
| 从完整旧库升级，安全更新原值0/1 | 两种情况均成功且恢复原值 |
| 预检阶段主动失败 | 原错误正常抛出、安全更新保持1、升级锁释放 |
| 回填阶段注入触发器错误，安全更新原值0/1 | 两种情况均恢复原值、释放锁，消除故障后重跑成功 |

使用此前缓存的旧库结构及小表数据，另注入100条旧格式scan_log。校验旧业务字段、历史时间、日志主键和内容，及重复升级后12表的逐行一致性。修复本身不增加任何数据库表、列或索引，也不改变日志保留策略。

实际执行记录：`tmp/test-db-upgrade-r5.py`；测试结果目录见工作区 `tmp/db-upgrade-r5-test-current.txt`。交付包同时提供 `r5-test-result.json` 和 SHA256SUMS。

## 以下为历史结构升级验收记录

# 旧库升级SQL验收（20260915修订3）

结论：27组隔离升级/应用验收全部通过；三个升级后数据库分别通过15项只读关联/结构检查。交付脚本覆盖截至2026-09-15已部署的当前后端数据库需求。本次未在27或149执行任何业务数据修改或升级DDL。

## 基线与范围

- 2026-09-15只读查询27和149的information_schema及SHOW CREATE TABLE，没有扫描两台服务器的大表内容。
- 27：MySQL 8.0.20、6张旧表；scan_log仍为旧5列，msg为VARCHAR(255)、created_date为DATETIME(3)。元数据估计行数为0，不能由此断定表为空。
- 149：MySQL 8.0.42、12张表；核对日志汇总12个新增字段、TEXT消息、5个索引、sys_builtin_session。
- 实际升级DDL运行在本机MySQL 8.0.45，ROW binlog、128 MiB缓冲池。使用旧库小表缓存快照（1197块缓冲垫、10台设备、34条PLC地址、1条寿命配置）、用户提供的结构SQL、121条真实历史明细抽样及合成大表数据。
- 没有把27完整历史下载到本机逐条验收；不把本机DDL通过等同于已在生产8.0.20执行过DDL。

## 验收结果

| 场景 | 结果 |
| --- | --- |
| 原始六表旧库直升 | 全部原字段保留；PLC映射、类型转换、寿命450、位置字典及初始admin正确 |
| 原用户结构SQL | wms_opc_str.sql精确导入后可升级 |
| 修订2补升修订3 | 可直接完成日志及会话差异，原有数据不改写 |
| 当前149结构 | 可重复执行，已有账号、密码、改密要求、寿命和位置设置保留 |
| 新版汇总数据 | 重跑保留长消息、operation_id、状态、设备快照、detail_json及更新时间 |
| 两类会话 | 数据库用户会话和内置账户会话均保留；不插入superamin账户 |
| 100万条scan_log | 所有原字段的流式SHA-256升级前后及重复执行后均一致 |
| 150121条cushion_detail | 原字段流式SHA-256一致；仅补缺少的scanner_id，不改历史位置/时间/open_count |
| 分批中断 | 第二批提交后注入故障，已填9902条关联保留；重跑完成且原数据完整 |
| 日志DDL后中断 | 失败不输出UPGRADE_OK；重跑可完成，不重复添加字段/索引 |
| 长消息 | 插入2000字符中文汇总通过；旧默认空msg插入通过 |
| 唯一操作ID | 重复非空ID插入被唯一索引拒绝，旧NULL记录并存 |
| 冲突预检 | 错误同名索引、过短快照字段、已有重复操作ID、superamin数据库同名记录均在业务改造前停止 |
| 原安全预检 | 重复扫码器、缺PLC/扫码器、地址类型冲突或未知、计数范围越界、明细UPDATE触发器/压缩行格式均正确拒绝 |
| 精度与历史 | 多产线关联、微秒/毫秒、未知旧扫码器、原位置及原始地址值保留 |
| 只读核验 | 大历史库、当前结构库、r2补升库，各15项violations均为0 |
| 当前应用启动 | 使用当前JAR启动升级后大历史库，admin和superamin可登录 |
| 百万日志分页 | 256 MiB Java堆下分页查询返回20条，总数1000002（100万历史+2条验收写入），没有旧版无界列表问题 |
| 主要查询API | 用户、寿命、设备、位置、PLC地址、缓冲垫、日志、操作事件、设备类型均通过 |

大表中断续跑本机耗时14.219秒；重复升级及保留校验13.187秒（含测试侧摘要读取）。这些时间仅代表本机环境，不保证现场耗时。日志表重建仍须停写、可恢复备份和足够磁盘空间。

应用测试前仅在隔离副本将设备地址替换为127.0.0.1:1，避免连接现场设备；关闭日志保留任务防止测试期间删除旧日志。未触发生产PLC、未执行生产清理。验收结束后本地数据库和后端进程已关闭。

## 证据

- 结构复核：工作区tmp/db-upgrade-r3-inspection/legacy.json、current.json。
- 27组验收：tmp/db-upgrade-r3-test-20260915-083433/result.json。
- 三库各15项只读检查：同目录verifier-result.json及各库*-verify.log。
- 完整控制台：tmp/db-upgrade-r3-tests.log。
- 可重跑脚本：tmp/test-db-upgrade-r3.py、tmp/verify-upgrade-r3-checks.py。
- SQL文件字节SHA-256：`bfb838e503a97dcc4774e9b2833c46205e14dc7719dd825e56d80bfcefe39e91`。
- mysql测试输入经换行规范化后的等价SQL SHA-256：`cbe16846f8dc5961b02b785c2b62335d39056bd21d9c1d7b848d1c77c7437c4b`。

具体执行步骤见同包20260915_旧库升级说明.md。旧r2包仍作为历史记录保留；请使用本次r3包执行完整升级。

后续修订：内置账号更名为 superadmin；本表为更名前的历史验收结果。本次修订验收见 `../设备状态与超级管理员更正验收.md`。
