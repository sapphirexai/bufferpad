# Database dump

Put the exported MySQL dump here before running `scripts/install.ps1`.

Default file name:

```text
wms_opc.sql
```

The installer imports this file into the `wms_opc` database after MySQL starts.
