# 开源迁移验证（2026-09-17）

**简体中文** · [English](migration-validation.en.md) · [文档目录](README.md)

本记录对应 304 条来源提交导入后的首次开源适配。迁移前的其他验收文档属于历史记录。

| 检查 | 结果 |
|---|---|
| 来源提交映射 | 前端 48、后端 243、安装程序 13，全部对应；保留 5 条清理后为空的提交 |
| 历史元数据与父子关系 | 304 条逐条通过 |
| 历史文件树 | 对所有来源提交应用排除、脱敏和目录前缀后逐条比对通过 |
| 前端依赖 | Node 18.19.0 下按锁文件安装成功；跳过旧 E2E 自动下载脚本 |
| 前端单元测试 | 16 个套件，65 项通过 |
| 前端生产构建 | 通过，使用旧 Webpack 所需的 OpenSSL 兼容参数 |
| 后端构建 | 使用公共 Maven 仓库和本地 HSL JAR 打包成功 |
| 后端测试 | 28 个套件，152 项通过，无失败、错误或跳过 |
| PowerShell | 安装脚本语法检查、应用制品准备、DryRun 模板及服务配置检查通过 |
| 卸载保护 | 无标记目录拒绝、已标记目录 DryRun 保留文件、驱动器根目录拒绝通过 |
| 缺失依赖与凭据 | 缺少 HSL 的构建入口明确报错；缺少初始密码的正式安装提前拒绝 |
| Markdown 链接 | 仓库文档本地链接检查通过 |

后端验证使用已有本地 HSL JAR，验证过程不赋予第三方再分发权。JAR、安装运行时、生成的前后端制品和日志不会随源码发布。

此次未执行真实 PLC 联调、正式 Windows 服务安装、真实 MySQL 旧库升级或 E2E 浏览器测试。下载脚本的全部第三方安装包也没有在此次迁移中重新下载验证。认证与权限集成测试使用独立 H2 内存数据库，协议测试使用本地回环或模拟设备。

旧库升级 SQL 已改为保留现有账户，并将空账户表的管理员初始化交给后端部署配置；没有对实际现场数据库执行这些脚本。

依赖安装与测试存在旧版工具的弃用、覆盖率模板等提示；它们没有导致本次构建或测试失败。后续依赖升级应独立验证。
